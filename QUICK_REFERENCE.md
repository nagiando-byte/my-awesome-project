# 📖 Zoom録画自動文字起こし - クイックリファレンス

## 🚀 すぐに使える!3つの方法

### 方法1: 手動で文字起こし
```bash
npm run transcribe -- -i recordings/your-video.mp4
```

### 方法2: フォルダを監視（手動起動）
```bash
npm run transcribe -- -w recordings
```

### 方法3: 完全自動（録画したら自動で文字起こし）⭐おすすめ
```bash
./start-auto-transcribe.sh
```
→ Zoomフォルダを監視して、録画完了を自動検知!

---

## 🎯 完全自動化の使い方

### 初回セットアップ
```bash
# 1. APIキー設定
./set-api-key.sh

# 2. 自動文字起こしを起動
./start-auto-transcribe.sh
```

### 使い方
1. **上記コマンドを実行**（ターミナルはそのまま）
2. **Zoomで録画**（いつも通り）
3. **録画終了**
4. **自動的に文字起こし開始!** 🎉
5. **macOS通知で完了をお知らせ** 🔔

### 停止
```bash
./stop-auto-transcribe.sh
```

---

## 📁 ファイル構成

```
my-awesome-project/
├── START_HERE.md              ← 📖 初めての方はこちら
├── AUTO_TRANSCRIBE.md         ← 🤖 完全自動化ガイド
├── QUICKSTART.md              ← ⚡ 5分セットアップ
├── README.md                  ← 📚 完全ドキュメント
│
├── set-api-key.sh             ← 🔑 APIキー設定
├── setup.sh                   ← ⚙️  環境チェック
│
├── start-auto-transcribe.sh   ← ▶️  自動監視を開始
├── stop-auto-transcribe.sh    ← ⏹️  自動監視を停止
├── status-auto-transcribe.sh  ← 📊 状態確認
├── enable-auto-start.sh       ← 🔄 ログイン時に自動起動
├── disable-auto-start.sh      ← 🚫 自動起動を無効化
│
└── recordings/                ← 📹 録画と結果がここに
```

---

## 🎬 ワークフロー

### パターンA: その都度使う
```bash
# 録画前に起動
./start-auto-transcribe.sh

# Zoomで録画

# 終了したいとき
Ctrl+C または ./stop-auto-transcribe.sh
```

### パターンB: 常時起動（おすすめ!）
```bash
# 一度だけ実行
./enable-auto-start.sh

# あとはmacOSを起動するだけでOK!
# Zoomで録画すれば自動的に文字起こし
```

---

## 💡 Tips

### Zoomの録画保存先を確認
Zoom → 設定 → 記録 → ローカル記録
→ デフォルト: `~/Documents/Zoom`

### 処理状況を確認
```bash
# リアルタイムでログを表示
tail -f auto-transcribe.log

# 状態確認
./status-auto-transcribe.sh
```

### 結果を確認
```bash
# recordingsフォルダを開く
open recordings/

# 最新の結果を表示
ls -lt recordings/*_transcript.json | head -1
```

---

## 📊 コマンド一覧

| コマンド | 説明 |
|---------|------|
| `./set-api-key.sh` | APIキー設定（初回のみ） |
| `./start-auto-transcribe.sh` | 自動監視開始 ⭐ |
| `./stop-auto-transcribe.sh` | 自動監視停止 |
| `./status-auto-transcribe.sh` | 状態確認 |
| `./enable-auto-start.sh` | ログイン時自動起動 |
| `npm run transcribe -- -i FILE` | 手動で文字起こし |

---

## ❓ よくある質問

**Q: 録画を検出しない**
A: `./status-auto-transcribe.sh`で起動を確認。Zoomの保存先が`~/Documents/Zoom`か確認。

**Q: バックグラウンドで実行できる?**
A: はい!`./start-auto-transcribe.sh &`でバックグラウンド実行可能。

**Q: 料金は?**
A: 1分あたり$0.006（約1円）。1時間で約50円と非常に安価。

**Q: 複数の録画を同時に処理できる?**
A: 順番に処理されます。同時処理はできません。

---

## 🎉 今すぐ始める!

```bash
# ステップ1: APIキー設定
./set-api-key.sh

# ステップ2: 自動監視開始
./start-auto-transcribe.sh

# ステップ3: Zoomで録画!
```

詳細は `AUTO_TRANSCRIBE.md` を参照してください。

